<main class="flex-grow flex justify-center items-center">
  <div class="mx-auto px-4 sm_px-8 text-center">
    <% if (products.length > 0) { %>
    <div class="text-center max-w-lg mx-auto mt-4">
      <h3
        class="text-black text-lg font-bold mb-8 text-3xl font-sans uppercase"
      >
        My Products
      </h3>
    </div>

    <div class="grid grid-cols-6 gap-4 items-start mt-8 mx-auto px-8">
      <% for( let product of products ) { %> <%- include('./card.ejs',
      {product:product}) %> <% }%>
    </div>
    <% } else { %>
    <div class="bg-white text-center shadow-md rounded px-8 pt-6 pb-8">
      <h3 class="block text-gray-700 text-3xl font-semibold">
        No Products
      </h3>
    </div>
    <% }%>
  </div>
</main>
